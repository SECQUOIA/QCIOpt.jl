var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Devices","page":"API Reference","text":"","title":"Devices"},{"category":"section","location":"api/#MathOptInterface-capabilities","page":"API Reference","text":"Capabilities follow the selected QCIOpt.DeviceType. Changing the device updates MOI.supports and MOI.supports_constraint immediately.\n\nMOI surface DIRAC-1 DIRAC-3\nScalar-quadratic objective yes yes\nVariableIndex or scalar-affine objective no yes\nScalar-nonlinear objective no no\nVariableIndex in ZeroOne yes yes\nVariableIndex in Integer no yes\nVariableIndex in EqualTo, Interval, GreaterThan, or LessThan no yes\nScalar or vector functional constraints no no\nMIN_SENSE / MAX_SENSE yes yes\nFEASIBILITY_SENSE no no\n\nDIRAC-1 accepts binary unconstrained quadratic models. DIRAC-3 has two distinct domain contracts. Without \"sum_constraint\", bound declarations describe finite integer domains: each non-fixed variable must also be Integer or ZeroOne; EqualTo and a zero-width Interval represent a fixed single level. With \"sum_constraint\" = R, every variable must instead be continuous, have lower bound zero, and have no upper bound; the native domain is the simplex xᵢ ≥ 0, Σxᵢ = R. Arbitrary continuous boxes, fixed/continuous mixtures, and integer/continuous mixtures are not supported. Scalar and vector functional constraints are not supported.\n\nBoth devices support MOI.ObjectiveSense for MIN_SENSE and MAX_SENSE, QCIOpt.DeviceType, MOI.Silent, and the raw optimizer attributes \"api_token\", \"device_type\", \"file_name\", \"num_samples\", \"job_name\", \"job_tags\", and \"silent\". DIRAC-3 additionally supports \"relaxation_schedule\" and \"sum_constraint\". Job parameters use the same names, ranges, and defaults documented in the README; DIRAC-3 derives num_levels from integer model domains and requires sum_constraint for a continuous simplex rather than exposing either device-domain representation as an arbitrary pass-through. The devices do not support MOI.TimeLimitSec, MOI.NumberOfThreads, or arbitrary raw attributes. MOI.Silent = true suppresses console output from file upload, job-body construction, and job processing while leaving solver results and provider metadata unchanged.","title":"MathOptInterface capabilities"},{"category":"section","location":"api/#Provider-metadata","page":"API Reference","text":"A solve keeps the QCI job response as the solution metadata verbatim, for both DIRAC-1 and DIRAC-3. QCIOpt.ProviderMetadata reads a normalized view of it back out:\n\nusing JuMP, QCIOpt\n\nmodel = Model(QCIOpt.Optimizer)\n# ... build and optimize the model ...\n\nmetadata = get_attribute(model, QCIOpt.ProviderMetadata())\n\nmetadata[\"job_id\"]         # provider job identifier\nmetadata[\"run_time_sec\"]   # same value as MOI.SolveTimeSec\nmetadata[\"error\"]          # provider job-error diagnostic, or nothing\nmetadata[\"response\"]       # the job response itself\n\nEvery key listed on QCIOpt.qci_provider_metadata is always present and is nothing when the response does not carry it, so reading one field never depends on another being reported. The provider status is also MOI.RawStatusString, and \"run_time_sec\" is MOI.SolveTimeSec. Neither fails on a partial response: a job that did not complete keeps its own provider status and reports NaN for the time, and stored metadata carrying no status string at all reports \"UNKNOWN\".","title":"Provider metadata"},{"category":"section","location":"api/#Relationship-to-the-QUBODrivers-sampler-metadata","page":"API Reference","text":"QCIOpt.DiracSampler publishes the same provider information under the standardized QUBODrivers sampler keys, which a benchmark harness reads from the SampleSet. The sampler derives its status, job-id, file-id, and device-usage entries from QCIOpt.qci_provider_metadata, so those cannot drift from the table above. The timing rows are a documented correspondence rather than shared code, because the two sides read timing from different provider sources — see the note after the table:\n\nProviderMetadata key DiracSampler sample-set metadata\n\"status\" metadata[\"status\"]\n\"job_id\" metadata[\"backend\"][\"job_id\"]\n\"result_file_id\" metadata[\"backend\"][\"result_file_id\"]\n\"problem_file_id\" metadata[\"backend\"][\"problem_file_id\"]\n\"run_time_sec\" (job-status timing; the sampler reports provider metrics under metadata[\"time\"])\n\"queue_time_sec\" (job-status timing; compare metadata[\"time\"][\"provider_queue\"])\n\"total_time_sec\" (job-status timing; compare metadata[\"time\"][\"provider_wall\"])\n\"device_usage_sec\" metadata[\"time\"][\"device_usage\"]\n\"error\" (raw response under metadata[\"provider\"])\n\"response\" metadata[\"provider\"][\"job_info\"] and siblings\n\nThe sampler additionally queries the provider's job-metrics endpoint, which the MOI path does not call: its \"time\" entries are nanosecond metrics reported by that endpoint, while the durations above are derived from the job-status timestamps that every job response carries. The sampler also records QUBODrivers' own algorithm, backend, and read-count fields, which describe the sampler contract rather than the provider.","title":"Relationship to the QUBODrivers sampler metadata"},{"category":"section","location":"api/#DIRAC-3-variable-transformation","page":"API Reference","text":"DIRAC-3 integer jobs sample each variable over consecutive levels starting at zero. QCIOpt.variable_domains states the full contract that maps a bounded integer JuMP/MOI model onto those levels and back.\n\nContinuous jobs are not a bounds transformation. Setting the raw optimizer attribute \"sum_constraint\" = R selects sample-hamiltonian and exposes the device's native simplex directly: every model variable must be declared continuous with xᵢ >= 0 and no upper bound, and the device samples Σxᵢ = R. Provider samples already use the model coordinates, so result reconstruction does not apply an inverse scaling. QCIOpt.continuous_sum_constraint states and validates this separate contract; QCIOpt.dirac3_job_type rejects mixed domains.","title":"DIRAC-3 variable transformation"},{"category":"section","location":"api/#QCI-service-helpers","page":"API Reference","text":"","title":"QCI service helpers"},{"category":"type","location":"api/#QCIOpt.QCI_DEVICE","page":"API Reference","text":"QCI_DEVICE\n\nAbstract type to represent QCI devices.\n\n\n\n\n\n","title":"QCIOpt.QCI_DEVICE"},{"category":"type","location":"api/#QCIOpt.DeviceType","page":"API Reference","text":"DeviceType()\n\nOptimizer attribute selecting the QCI device backend. Supported values are the keys returned by qci_supported_devices, such as \"dirac-1\" and \"dirac-3\".\n\n\n\n\n\n","title":"QCIOpt.DeviceType"},{"category":"function","location":"api/#QCIOpt.qci_device","page":"API Reference","text":"qci_device(spec::AbstractString; kwargs...)\n\n\n\n\n\n","title":"QCIOpt.qci_device"},{"category":"function","location":"api/#QCIOpt.qci_device_type","page":"API Reference","text":"qci_device_type(spec::AbstractString)\n\nReturns the equivalent QCI_DEVICE type to a given spec string.\n\n\n\n\n\n","title":"QCIOpt.qci_device_type"},{"category":"function","location":"api/#QCIOpt.qci_supports_device","page":"API Reference","text":"qci_supports_device(spec::AbstractString)::Bool\n\nTells whether the solver supports a given device spec.\n\n\n\n\n\n","title":"QCIOpt.qci_supports_device"},{"category":"function","location":"api/#QCIOpt.qci_supported_devices","page":"API Reference","text":"qci_supported_devices\n\n\n\n\n\n","title":"QCIOpt.qci_supported_devices"},{"category":"type","location":"api/#QCIOpt.ProviderMetadata","page":"API Reference","text":"ProviderMetadata()\n\nOptimizer attribute holding the provider metadata preserved from the QCI job response of the last solve, for both DIRAC-1 and DIRAC-3.\n\nMOI.get(solver, QCIOpt.ProviderMetadata()) returns the normalized dictionary documented on qci_provider_metadata: job identity, provider status, job-status timing, result and problem file ids, the provider's job-error diagnostic, and the job response itself under \"response\". Every key is always present and is nothing when the response does not carry it, including before optimize! has been called.\n\nThe provider status is also available as MOI.RawStatusString, and the running time as MOI.SolveTimeSec; this attribute is the only access to the remaining fields.\n\n\n\n\n\n","title":"QCIOpt.ProviderMetadata"},{"category":"function","location":"api/#QCIOpt.qci_provider_metadata","page":"API Reference","text":"qci_provider_metadata(response)\n\nNormalized view of the provider fields QCIOpt preserves from a QCI job response, as a Dict{String,Any} with these keys:\n\nKey Source in the provider response\n\"status\" status\n\"job_id\" job_info.job_id\n\"result_file_id\" job_info.job_result.file_id\n\"problem_file_id\" job_info.job_submission.problem_config.*.*_file_id\n\"queue_time_sec\" job_info.job_status, queued → running\n\"run_time_sec\" job_info.job_status, running → completed\n\"total_time_sec\" job_info.job_status, submitted → completed\n\"device_usage_sec\" job_info.job_result.device_usage_s\n\"error\" job_info.job_result.error\n\"response\" the response itself, verbatim\n\nEvery key is always present; a value the response does not carry is nothing, so reading one field never depends on another being there. \"response\" keeps the unnormalized job response reachable, since the provider may report fields this table does not name.\n\nUnlike every other value here, \"response\" is not a fresh object: it aliases the solution metadata the solver holds, so it must not be mutated, and MOI.empty! invalidates it by emptying that dictionary in place.\n\nThis is the same provider information the QUBODrivers sampler publishes under its own standardized keys; see QCIOpt.DiracSampler and the API reference for the key-by-key correspondence.\n\n\n\n\n\n","title":"QCIOpt.qci_provider_metadata"},{"category":"function","location":"api/#QCIOpt.qci_response_field","page":"API Reference","text":"qci_response_field(value, path::AbstractString...)\n\nRead a nested field out of a provider response, returning nothing when any step of path is absent or the value at that step is not a dictionary.\n\nEvery field of a QCI job response other than \"status\" is optional as far as this package is concerned: the provider fills job_info, job_status, and job_result progressively, and a job that never ran carries none of them. So derived metadata reads through this helper instead of indexing directly, which is what keeps a missing provider field from turning into a KeyError inside an unrelated MOI.get call.\n\n\n\n\n\n","title":"QCIOpt.qci_response_field"},{"category":"function","location":"api/#QCIOpt.qci_problem_file_id","page":"API Reference","text":"qci_problem_file_id(response)\n\nThe uploaded problem-file id recorded on a job submission, or nothing when the response carries no submission. Works for both the DIRAC-1 QUBO and DIRAC-3 polynomial job types.\n\n\n\n\n\n","title":"QCIOpt.qci_problem_file_id"},{"category":"function","location":"api/#QCIOpt.qci_provider_error","page":"API Reference","text":"qci_provider_error(response)\n\nThe provider's job-error diagnostic, or nothing when the response carries none. An ERRORED job normally reports it, but the field is read defensively so a job that errored without one still reaches the caller as ERRORED rather than as an indexing error.\n\n\n\n\n\n","title":"QCIOpt.qci_provider_error"},{"category":"function","location":"api/#QCIOpt.qci_get_elapsed_time","page":"API Reference","text":"qci_get_elapsed_time(job_status)\n\nSeconds the provider spent running a job, from its running_at_ to its completed_at_ job-status timestamp, or NaN when the timestamps are missing or unusable. NaN is the same value MOI.SolveTimeSec reports for a job that did not complete, so a completed job whose timing the provider did not report leaves the rest of the solution readable.\n\n\n\n\n\n","title":"QCIOpt.qci_get_elapsed_time"},{"category":"function","location":"api/#QCIOpt.qci_elapsed_seconds","page":"API Reference","text":"qci_elapsed_seconds(job_status, from_prefix::AbstractString, to_prefix::AbstractString)\n\nSeconds between two job_status timestamps, or nothing when either timestamp is missing or unparseable, or when the interval runs backwards. Resolution is milliseconds (see qci_parse_timestamp).\n\n\n\n\n\n","title":"QCIOpt.qci_elapsed_seconds"},{"category":"function","location":"api/#QCIOpt.qci_status_timestamp","page":"API Reference","text":"qci_status_timestamp(job_status, prefix::AbstractString)\n\nRead the single job_status timestamp whose key starts with prefix (for example \"running_at_\"), returning nothing when job_status is not a dictionary, when no key or more than one key matches, or when the value is not a parseable timestamp. The key suffix is not pinned because it names the provider encoding (rfc3339nano today).\n\n\n\n\n\n","title":"QCIOpt.qci_status_timestamp"},{"category":"function","location":"api/#QCIOpt.qci_parse_timestamp","page":"API Reference","text":"qci_parse_timestamp(value)\n\nParse one QCI job-status timestamp into a DateTime, or return nothing when value is not a timestamp this package can compare.\n\nThe provider reports job-status timestamps under rfc3339nano keys, so the fractional part can carry more precision than DateTime represents; it is truncated to milliseconds. A timestamp carrying an explicit numeric UTC offset is rejected rather than parsed as if it were UTC, because a duration taken across two differently-offset timestamps would be wrong by whole hours without any sign of it in the result.\n\n\n\n\n\n","title":"QCIOpt.qci_parse_timestamp"},{"category":"function","location":"api/#QCIOpt.qci_parse_results","page":"API Reference","text":"qci_parse_results(::Type{U}, ::Type{T}, response) where {U,T}\n\nTurn a QCI job response into a Solution: the sampled points for a COMPLETED job, and no samples for any other provider status. The response is kept verbatim as the solution metadata in every case, which is what preserves the job identity, timing, and diagnostic fields that qci_provider_metadata reads back out.\n\nOnly status is required of the response. Every other field is optional, so an ERRORED job that carries no diagnostic message still returns with its status intact instead of failing on a missing field.\n\n\n\n\n\n","title":"QCIOpt.qci_parse_results"},{"category":"function","location":"api/#QCIOpt.qci_provider_results","page":"API Reference","text":"qci_provider_results(response)\n\nReturn the results payload of a COMPLETED job response, checking that it carries the sample fields QCIOpt reads and that they agree on length.\n\nA COMPLETED job always carries results, so a response that does not is a provider-contract violation rather than an optional field: it fails here, naming the reported status and the offending field, instead of surfacing as a KeyError or a length mismatch from deeper in the parse.\n\n\n\n\n\n","title":"QCIOpt.qci_provider_results"},{"category":"function","location":"api/#QCIOpt.variable_domains","page":"API Reference","text":"variable_domains(solver::Optimizer{T}, device::DIRAC_3{T}, vars) where {T}\n\nReturn the integer domain (lᵢ, uᵢ) that a DIRAC-3 sample-hamiltonian-integer job samples for each variable in vars, in the order given, validating the model against what that job type can represent. Continuous models use the separate continuous_sum_constraint contract and are not passed to this function by the production path.\n\nDIRAC-3 receives sample-hamiltonian-integer jobs, whose variable i takes the num_levelsᵢ consecutive integer values 0, 1, …, num_levelsᵢ - 1. The transformation contract is therefore:\n\nevery variable is integer-valued and box-bounded, so its domain is the integer interval [lᵢ, uᵢ] = [ceil(lowerᵢ), floor(upperᵢ)];\nthe objective is submitted after the substitution xᵢ ↦ xᵢ + lᵢ (rescale_variables), which moves that domain onto [0, uᵢ - lᵢ];\nthe device is told num_levelsᵢ = uᵢ - lᵢ + 1 (get_levels);\na returned point yᵢ maps back as xᵢ = yᵢ + lᵢ (readjust_poly_values), inverting step 2.\n\nSteps 2 and 4 must use the same lᵢ computed here, which is why each step derives its bounds from this function rather than from solver.lower directly: for an Int variable declared over fractional bounds the sampled lattice starts at ceil(lowerᵢ), not at lowerᵢ.\n\nThrows an ErrorException naming the offending variable when a domain is unbounded, infinite, free of integer points, continuous, or wider than the machine integer range can count.\n\n\n\n\n\n","title":"QCIOpt.variable_domains"},{"category":"function","location":"api/#QCIOpt.continuous_sum_constraint","page":"API Reference","text":"continuous_sum_constraint(solver::Optimizer{T}, device::DIRAC_3{T}, vars) where {T}\n\nValidate and return the resource R for a native continuous DIRAC-3 simplex, x_i ge 0 and sum_i x_i = R. Set R through the raw optimizer attribute \"sum_constraint\" (a finite real number in [1, 10000]).\n\nEvery model variable must be continuous, carry a lower bound of exactly zero, and have no upper or fixed bound. Arbitrary boxes cannot be transformed into the device's one-resource simplex and are rejected rather than silently changed.\n\n\n\n\n\n","title":"QCIOpt.continuous_sum_constraint"},{"category":"function","location":"api/#QCIOpt.dirac3_job_type","page":"API Reference","text":"dirac3_job_type(solver::Optimizer, device::DIRAC_3, vars)\n\nChoose the native DIRAC-3 job type from the model domains. Models are either entirely integer/fixed (sample-hamiltonian-integer) or entirely continuous (sample-hamiltonian). Mixed integer-continuous models are not a domain the device can sample and fail with an actionable error.\n\n\n\n\n\n","title":"QCIOpt.dirac3_job_type"},{"category":"function","location":"api/#QCIOpt.qci_build_poly_request","page":"API Reference","text":"qci_build_poly_request(solver::Optimizer{T}, device::DIRAC_3{T}, vars; file_name = nothing) where {T}\n\nBuild everything a DIRAC-3 submission needs from a loaded model, without touching the network. Integer models produce a shifted polynomial and per-variable level counts; continuous models produce the unchanged polynomial and native simplex sum constraint. This is the whole model-to-request half of qci_optimize!, split out so it can be exercised offline.\n\nApplies the contracts documented on variable_domains and continuous_sum_constraint, and so raises their domain errors. Being network-free is what lets an unusable model fail with its own error rather than with a credentials or connectivity error; the integer level budget is checked separately by assert_level_budget, because the allocation limit itself has to be read from the provider.\n\nReturns a named tuple (; job_type, poly, file, num_levels, sum_constraint); exactly one of num_levels and sum_constraint is non-nothing.\n\n\n\n\n\n","title":"QCIOpt.qci_build_poly_request"},{"category":"function","location":"api/#QCIOpt.rescale_variables","page":"API Reference","text":"rescale_variables(p::Poly{T}, vars::AbstractVector{PolyVar}, l::AbstractVector{T}) where {T}\n\nShift the domain of p so that each variable starts at zero, returning the polynomial the device actually receives.\n\nThe device samples yᵢ ∈ [0, uᵢ - lᵢ], while the model variable lives on xᵢ ∈ [lᵢ, uᵢ]. Submitting p(y + l) — the substitution xᵢ ↦ xᵢ + lᵢ — is what makes the two agree, because the inverse mapping applied to a returned point is xᵢ = yᵢ + lᵢ (see readjust_poly_values). Substituting xᵢ ↦ xᵢ - lᵢ instead would submit p(y - l), so the device would optimize over [2lᵢ, uᵢ] and the mapped-back point would not be the argument that produced the sampled value.\n\nVariables with lᵢ == 0 are skipped, since the shift is the identity there.\n\n\n\n\n\n","title":"QCIOpt.rescale_variables"},{"category":"function","location":"api/#QCIOpt.get_levels","page":"API Reference","text":"get_levels(domains::AbstractVector{Tuple{Int,Int}})\nget_levels(solver::Optimizer{T}, device::DIRAC_3{T}, vars) where {T}\n\nNumber of levels DIRAC-3 allocates per variable: one per integer point of the transformed domain [0, uᵢ - lᵢ]. See variable_domains.\n\n\n\n\n\n","title":"QCIOpt.get_levels"},{"category":"function","location":"api/#QCIOpt.assert_level_budget","page":"API Reference","text":"assert_level_budget(num_levels::AbstractVector{<:Integer}, limit::Integer)\n\nCheck the per-variable level counts against the total level budget of the current allocation (see qci_max_level), which the device enforces across all variables of a job.\n\nThe total is accumulated in BigInt: per-variable counts that are individually representable can still sum past typemax(Int), and a wrapped negative total would compare below any budget and let the job through.\n\n\n\n\n\n","title":"QCIOpt.assert_level_budget"},{"category":"function","location":"api/#QCIOpt.readjust_poly_values","page":"API Reference","text":"readjust_poly_values(solver::Optimizer{T}, device::DIRAC_3{T}, vars, samples::Vector{Sample{T,T}}, sense) where {T}\n\nMap provider sample points back to the original variable domain and recompute objective values from the stored polynomial, restoring the original model's objective for MAX_SENSE models. Integer-qudit samples add each variable's transformed lower bound (see variable_domains); continuous simplex samples already use the model coordinates and need no inverse transformation. Returns the samples ordered best-first for the given sense.\n\n\n\n\n\n","title":"QCIOpt.readjust_poly_values"},{"category":"function","location":"api/#QCIOpt.qci_build_job_body","page":"API Reference","text":"qci_build_job_body(solver::Optimizer, device::DIRAC_1; file_id, api_token, silent)\n\nBuild a DIRAC-1 job body from the validated raw optimizer attributes stored on solver. This is the network-free caller-to-client boundary used by qci_optimize!.\n\n\n\n\n\n","title":"QCIOpt.qci_build_job_body"},{"category":"function","location":"api/#QCIOpt.qci_build_poly_job_body","page":"API Reference","text":"qci_build_poly_job_body(file_id; device_type, job_type, num_levels, sum_constraint, kwargs...)\n\nBuild a QCI polynomial job body from explicit client and job arguments. num_levels configures an integer-qudit job, while sum_constraint configures a continuous normalized-qudit job.\n\n\n\n\n\nqci_build_poly_job_body(solver::Optimizer, device::DIRAC_3; file_id, num_levels, sum_constraint, api_token, silent)\n\nBuild a DIRAC-3 polynomial job body from the validated raw optimizer attributes stored on solver. Exactly one of num_levels (integer-qudit mode) and sum_constraint (continuous normalized-qudit mode) is supplied. This is the network-free caller-to-client boundary used by qci_optimize!.\n\n\n\n\n\n","title":"QCIOpt.qci_build_poly_job_body"},{"category":"function","location":"api/#QCIOpt.qci_is_free_tier","page":"API Reference","text":"qci_is_free_tier(; url = QCI_URL, api_token = qci_default_token(), silent = false)\n\nReturn true when the configured QCI allocation is on the free tier. Set silent = true to suppress provider console output while reading the allocation.\n\n\n\n\n\n","title":"QCIOpt.qci_is_free_tier"},{"category":"function","location":"api/#QCIOpt.qci_max_level","page":"API Reference","text":"qci_max_level(::DIRAC_3; url = QCI_URL, api_token = qci_default_token(), silent = false)\n\nReturn the DIRAC-3 total-level budget for the configured allocation: 500 on the free tier and 949 on the paid tier. Set silent = true to suppress provider console output while reading the allocation.\n\n\n\n\n\n","title":"QCIOpt.qci_max_level"},{"category":"function","location":"api/#QCIOpt.qci_process_job","page":"API Reference","text":"qci_process_job(job_body; url = QCI_URL, api_token = qci_default_token(), silent = false, verbose = !silent)\n\nSubmit and process a QCI job. When silent is true, provider console output is captured and not displayed; the same setting disables the client's progress messages by default.\n\n\n\n\n\n","title":"QCIOpt.qci_process_job"},{"category":"section","location":"manual/1-introduction/#Introduction","page":"Introduction","text":"","title":"Introduction"},{"category":"section","location":"manual/2-examples/#Examples","page":"Examples","text":"using QCIOpt\nusing JuMP\n\nmodel = Model(QCIOpt.Optimizer)\n\n@variable(model, -10 <= x[1:5] <= 4, Int)\n\n@objective(model, Min, sum((-1)^(i + j) * x[i] * x[j] for i = 1:3 for j = 1:3))\n\nmodel","title":"Examples"},{"category":"section","location":"#QCIOpt-Documentation","page":"Home","text":"","title":"QCIOpt Documentation"}]
}
